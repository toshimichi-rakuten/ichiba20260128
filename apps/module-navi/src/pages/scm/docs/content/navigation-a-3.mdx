
import Demo from 'src/components/Demo'
import Code from 'src/components/Code'
import Html from 'src/components/Html'
import Space from 'src/components/Space'
import { Tabs } from '@mantine/core'
import defaultHtml from 'scm/src/modules/content-card/padded.html'

export const meta = {
  title: '下層導線 A-3 通常 枠あり',
  description: '画像(比率1:1)＋メインカテゴリ名＋説明文(省略可)',
  url: '/scm/docs/content/navigation-a-3',
  tags: ['navigation'],
}

# 下層導線 A-3 通常 枠あり

画像(比率1:1)＋メインカテゴリ名＋説明文(省略可)

<Space />

<Demo scm={true} content={defaultHtml} breakpoints={['sp', 'md', 'lg']} />



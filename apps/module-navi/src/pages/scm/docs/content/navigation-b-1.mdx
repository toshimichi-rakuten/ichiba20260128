
import Demo from 'src/components/Demo'
import Code from 'src/components/Code'
import Html from 'src/components/Html'
import Space from 'src/components/Space'
import { Tabs } from '@mantine/core'
import defaultHtml from 'scm/src/modules/content-banner/side.html'

export const meta = {
  title: '下層導線 B-1 画像背景 横長',
  description: '画像(375×211)＋メインカテゴリ名＋説明文(省略可)',
  url: '/scm/docs/content/navigation-b-1',
  tags: ['navigation'],
}

# 下層導線 B-1 画像背景 横長

画像(375×211)＋メインカテゴリ名＋説明文(省略可)

<Space />

<Demo scm={true} content={defaultHtml} breakpoints={['sp', 'md', 'lg']} />



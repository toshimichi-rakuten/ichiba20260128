
import Demo from 'src/components/Demo'
import Code from 'src/components/Code'
import Html from 'src/components/Html'
import Space from 'src/components/Space'
import { Tabs } from '@mantine/core'
import floatingButtonDemoHtml from 'scm/src/modules/modal/floating-button.html'

export const meta = {
  title: '右下フローティング_モーダル + モーダルボタン',
  url: '/scm/docs/navi/floating-button',
  tags: ['floating', 'modal'],
}

# 右下フローティング_モーダル + モーダルボタン

<Tabs
  keepMounted={false}
  defaultValue='sp'
  styles={{ panel: { paddingTop: '16px' } }}
>
  <Tabs.List>
    <Tabs.Tab value='sp'>SP</Tabs.Tab>
    <Tabs.Tab value='md'>MD</Tabs.Tab>
    <Tabs.Tab value='lg'>PC</Tabs.Tab>
    <Tabs.Tab value='code'>コード</Tabs.Tab>
  </Tabs.List>

  <Tabs.Panel value='sp'>
    <Html
      scm={true}
      content={`<div style="height: 1500px">👇👇👇 Scroll 👇👇👇</div>${floatingButtonDemoHtml}`}
    />
  </Tabs.Panel>

  <Tabs.Panel value='md'>
    <Html
      scm={true}
      device={'md'}
      content={`<div style="height: 1500px">👇👇👇 Scroll 👇👇👇</div>${floatingButtonDemoHtml}`}
    />
  </Tabs.Panel>

  <Tabs.Panel value='lg'>
    <Html
      scm={true}
      device={'lg'}
      content={`<div style="height: 1500px">👇👇👇 Scroll 👇👇👇</div>${floatingButtonDemoHtml}`}
    />
  </Tabs.Panel>

  <Tabs.Panel value='code'>
    <Code content={floatingButtonDemoHtml} />
  </Tabs.Panel>
</Tabs>

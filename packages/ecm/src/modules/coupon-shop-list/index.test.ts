/**
 * @vitest-environment jsdom
 */
import { describe, it, expect } from 'vitest'

// TODO: After spec confirmation.
describe('CouponShopList', () => {
  it('should work', () => {
    expect(true).toBe(true)
  })
})
